# PowerSignal 데이터 수집 — Claude Code 핸드오프 문서

> 이 문서는 PowerSignal 서비스의 데이터 수집 파이프라인 구현을 위한 핸드오프 문서다.
> 기획 문서(`주제 구체화`, `태희`)를 분석하여 필요 데이터셋·수집 방식·구현 플랜을 정리했다.
> 작성 시점에 공공데이터 제공 형태를 검증한 결과를 반영했다.

---

# 태희 사랑해

## 0. 프로젝트 한 줄 요약

**PowerSignal**: 전력시장 공공데이터를 AI가 분석해 "전기가 언제 비싸지고(SMP), DR 참여가 언제 유리한지(DR 낙찰 가능성)"를 **업종별 맥락**으로 점수화·설명해주는 에너지 의사결정 지원 웹 서비스.

이 문서가 다루는 범위는 **데이터 수집(ingestion) 계층**에 한정된다. 모델링/스코어링/프런트엔드는 별도.

서비스의 3대 기능과 이를 떠받치는 데이터:

| 기능 | 설명 | 핵심 데이터 |
|---|---|---|
| SMP 예측 스코어 | 내일 시간대별 전기값 예측 → 0~100 점수 | SMP+수요예측, 기상, LNG 연료비 |
| DR 낙찰 가능성 스코어 | 지금 입찰하면 낙찰될 확률 점수화 | DR 거래실적(낙찰 패턴 학습), 실시간 수급현황 |
| 업종별 분석 | 같은 시장상황을 업종 맥락으로 해석 | 업종별 에너지 사용 통계(정적), 업종 적합도(직접 정의) |

---

## 1. 핵심 도메인 용어 (코드 작성자가 알아야 할 최소 배경)

- **SMP (계통한계가격, System Marginal Price)**: 전력 도매시장에서 1시간 단위로 정해지는 전기 도매가격(원/kWh). 시간대마다 다르다. 새벽엔 싸고 낮·저녁 피크엔 비싸다. 하루 전(D-1) 23시경 다음 날 시간별 값이 공개된다.
- **DR (수요반응, Demand Response)**: 전력이 부족할 때 수요처(공장)가 자발적으로 전기 사용을 줄이고 대가를 받는 제도. **입찰제이며 낙찰이 보장되지 않는다.** → "언제 입찰하면 낙찰될까"가 핵심 문제.
  - **경제성 DR**: 시장 가격(SMP) 기반으로 경제성에 따라 발동되는 DR. → **낙찰 패턴 학습의 주 대상.**
  - **신뢰성 DR**: 전력 비상·수급 불안 시 발동되는 DR.
- **계통 예비력**: 현재 전력 공급 여유분(MW). 예비력이 빠듯할수록 DR 발령 가능성↑.
- **업종별 에너지 집중도**: 업종이 전기를 얼마나 집약적으로 쓰는지. 철강·금속처럼 높은 업종은 SMP 변동에 원가가 민감.

---

## 2. 필요 데이터셋 전체 목록

수집 방식·자동화 난이도 기준으로 3그룹으로 분류한다. (실시간성이 아니라 "어떻게 가져오느냐"가 구현 관점에서 더 중요한 분류축이다.)

### 그룹 1 — OpenAPI 자동 수집 (난이도: 낮음)

공공데이터포털에서 활용신청 → 서비스키 발급 → REST API(JSON/XML) 호출. 매일 도는 스케줄러로 자동화 적합.

| ID | 데이터셋 | 제공기관 | 주요 변수 | 갱신주기 | URL |
|---|---|---|---|---|---|
| D1 | 계통한계가격 및 수요예측 (하루전 발전계획용) | 전력거래소 | 거래일·거래시간·지역(육지/제주)·SMP·수요예측 | 1일 1회 23시경 | data.go.kr/data/15131225/openapi.do |
| D2 | 현재전력수급현황조회 | 전력거래소 | 공급능력·현재수요·예비력·공급예비율 | 실시간성 | data.go.kr/data/15056640/openapi.do |
| D3 | 기상 데이터 (ASOS 지상관측) | 기상청 | 지점·일시·기온·강수량·풍속·풍향·습도·기압 | 1일 1회 22시(전일) | data.kma.go.kr |
| D4 | 월간 연료비용 정보 (LNG 가격) | 공공데이터포털 | 연료단가·열량단가·연료비단가 | 월 1회 | data.go.kr/data/15099765/openapi.do |

**⚠️ 중요 — D1 API 버전 주의:**
- 구버전 `계통한계가격조회 API(15076302)`는 **추후 삭제 예정**. 신버전 `계통한계가격 및 수요예측(하루전 발전계획용)(15131225)` 사용 권장.
- 기획 문서에 구버전 URL이 적혀 있다면 신버전으로 교체할 것.
- 거래시간 표기 주의: 거래시간 06시 = 05:00 직후~06:00 종료 구간(단위기간의 끝점 표기).

**⚠️ 트래픽 제한:**
- 개발계정 기본 트래픽 100건/일. 개발·테스트엔 충분.
- 실서비스는 운영계정 전환(활용사례 등록) 필요.

### 그룹 2 — 파일 다운로드 (CSV/엑셀) (난이도: 중간)

OpenAPI가 아니라 파일데이터로만 제공. 로그인 없이 다운로드 가능하나, 자동화하려면 다운로드 URL 처리 또는 주기적 갱신 로직 필요.

| ID | 데이터셋 | 제공기관 | 용도 | 갱신주기 | URL |
|---|---|---|---|---|---|
| D5 | 수요반응자원 거래실적 (경제성DR) | 전력거래소 | **DR 낙찰 패턴 학습 핵심** | 월별 합산, D-2월 정산 | 공공데이터포털 |
| D6 | 수요반응자원 거래실적 (신뢰성DR) | 전력거래소 | 비상·수급불안 시 DR 분석 | 월별 합산, D-2월 정산 | data.go.kr/data/15069360 |

### 그룹 3 — 별도 신청 / 직접 가공 (난이도: 높음 · 차별점 핵심)

**여기가 서비스 차별점(DR 낙찰 예측·업종별 분석)이 걸린, 동시에 가장 손이 많이 가는 구간이다.** 일정 초반에 리스크 해소 필요.

| ID | 데이터셋 | 상태 | 비고 |
|---|---|---|---|
| D7 | 시간별 경제성/플러스DR 입찰량/낙찰량 | **별도 신청 가능성** | 전력거래소 "공공데이터 큐레이션 서비스"에 API 요청 신청 사례 존재. 시간 단위 학습 데이터가 필요하면 신청 절차·처리시간 확인 필수 |
| D8 | 산업부문 에너지사용 및 온실가스배출량 통계 | 파일데이터(존재) | 업종·연료·광역시도별 구분. **업종별 집중도 지수는 직접 산출해야 함** |
| D9 | 에너지사용량 통계 (다소비사업자 신고) | 파일데이터(존재) | 2,000toe 이상 사업장 의무신고. 사업장별 연간 소비량 |
| D10 | 업종별 DR 참여 적합도 / 공정 특성 | **공공데이터 없음** | 별점·참여가능공정은 도메인 지식. 직접 정의 또는 논문·산업자료 근거 필요 |

**⚠️ 그룹 3 핵심 주의사항:**
1. D8/D9는 갱신주기가 **연 1회** → 실시간 파이프라인이 아니라 **구축 시점 1회 수집 후 가공해 박아두는 정적 기준 데이터**로 취급. 매일 수집 스크립트에 넣지 말 것.
2. 기획서의 "집중도 지수 3.8" 같은 숫자는 원시 통계에 없다. **원시 통계 → 파생 지표 산출 로직을 별도 구현**해야 함.
3. D10(DR 적합도 별점)은 어떤 공공데이터에도 없다. 기획팀이 정의할 도메인 영역.

---

## 3. 데이터 파이프라인 아키텍처

```
[수집 계층 — 이 문서 범위]
                          ┌─────────────────────────────────────┐
   매일 23시 이후 ───────▶│ Daily Job (그룹1 API)               │
                          │  D1 SMP+수요예측 / D2 수급 / D3 기상 │
                          └──────────────┬──────────────────────┘
                                         │
   월 1회 ────────────────▶ Monthly Job (D4 LNG, D5/D6 DR 거래실적)
                                         │
   1회성/연1회 ───────────▶ Static Load (D7 신청분, D8/D9 업종통계)
                                         │
                                         ▼
                          ┌─────────────────────────────────────┐
                          │ Raw Storage (원본 JSON/CSV 보존)     │
                          └──────────────┬──────────────────────┘
                                         ▼
                          ┌─────────────────────────────────────┐
                          │ 정제·통합 (시간 단위 데이터셋 구축)  │
                          │  - 시간키 정규화(거래시간 끝점 주의)  │
                          │  - 지역(육지/제주) 구분 유지          │
                          │  - 결측·갱신지연 처리                │
                          └──────────────┬──────────────────────┘
                                         ▼
                          [모델링 계층 — 별도 작업]
                          SMP 예측 / DR 낙찰 예측 / 업종 가중치 적용
```

---

## 4. 구현 플랜 (Phase 단위)

### Phase 0 — 환경 셋업 & 인증
- [ ] 공공데이터포털 회원가입 + 활용신청 (D1~D4 각각)
- [ ] 서비스키 발급, `.env`에 보관 (절대 커밋 금지)
- [ ] 기상청 ASOS는 별도 인증 체계일 수 있음 — 확인
- [ ] **D7 큐레이션 서비스 신청 즉시 착수** (처리 시간 소요 → 가장 먼저)
- [ ] 프로젝트 구조 셋업 (아래 권장 구조 참고)

### Phase 1 — 그룹1 API 수집기 구현 (자동화 우선)
- [ ] 공통 API 클라이언트 (서비스키 주입, 재시도, 레이트리밋, 에러 핸들링)
- [ ] D1 SMP+수요예측 수집기 → 거래시간 정규화, 육지/제주 분리 저장
- [ ] D2 실시간 수급현황 수집기
- [ ] D3 기상 데이터 수집기 (지점 코드 매핑 필요)
- [ ] D4 LNG 연료비 수집기 (월 1회)
- [ ] 원본 응답 raw 보존 + 파싱본 분리 저장
- [ ] 스케줄러 (cron 또는 airflow/prefect) — D1은 23시 이후 실행

### Phase 2 — 그룹2 파일 데이터 수집
- [ ] D5/D6 DR 거래실적 다운로드 자동화 (다운로드 URL 추적 또는 수동 갱신 절차 문서화)
- [ ] CSV/엑셀 인코딩 처리 (euc-kr 가능성 주의), 스키마 정규화
- [ ] 월별 데이터 누적 적재 (D-2월 정산 지연 반영)

### Phase 3 — 그룹3 정적·신청 데이터
- [ ] D7 시간별 DR 데이터 (신청 승인 후 수집기 구현)
- [ ] D8/D9 업종통계 1회 로드 + 정제
- [ ] **업종별 집중도 지수 산출 로직 구현** (방법론은 기획/모델팀과 합의 후)
- [ ] D10 업종 적합도 테이블 (기획팀 정의값을 코드 상수/설정 파일로 관리)

### Phase 4 — 통합 데이터셋 구축
- [ ] 시간 단위 통합 테이블 생성 (SMP·수요·예비율·기상·LNG 조인)
- [ ] DR 거래실적을 학습 가능한 형태로 라벨링 (조건→낙찰여부/낙찰률)
- [ ] 데이터 품질 검증 (결측·이상치·갱신지연 리포트)

---

## 5. 권장 프로젝트 구조

```
powersignal-ingestion/
├── .env                      # 서비스키 (gitignore)
├── .env.example              # 키 이름만 명시
├── config/
│   ├── datasets.yaml         # 데이터셋별 엔드포인트·파라미터 정의
│   └── industry_dr.yaml      # D10 업종 적합도 (기획팀 정의값)
├── src/
│   ├── clients/
│   │   └── data_go_kr.py     # 공통 API 클라이언트
│   ├── collectors/
│   │   ├── smp.py            # D1
│   │   ├── supply.py         # D2
│   │   ├── weather.py        # D3
│   │   ├── lng.py            # D4
│   │   └── dr_history.py     # D5/D6
│   ├── transform/
│   │   ├── time_normalize.py # 거래시간 끝점 처리
│   │   └── industry_index.py # 업종 집중도 지수 산출
│   └── storage/
│       └── ...               # raw / parsed 저장
├── data/
│   ├── raw/                  # 원본 응답 보존
│   ├── parsed/               # 정제본
│   └── static/               # 업종통계 등 정적 데이터
├── scripts/
│   └── run_daily.py
└── tests/
```

---

## 6. 구현 시 반드시 챙길 주의사항 (체크리스트)

- [ ] **D1 API 신버전(15131225) 사용** — 구버전은 삭제 예정
- [ ] **거래시간 끝점 표기** — 06시 = 05:00~06:00 구간. 시간키 정규화 시 off-by-one 주의
- [ ] **육지/제주 지역 구분** — D1은 두 지역 모두 제공. 서비스 대상에 따라 필터 또는 둘 다 보존
- [ ] **서비스키 보안** — `.env` 분리, 절대 커밋 금지
- [ ] **트래픽 제한** — 개발 100건/일. 백필(과거 데이터 대량 수집) 시 레이트리밋 로직 필수
- [ ] **CSV 인코딩** — 한국 공공기관 파일은 euc-kr/cp949 가능성. 인코딩 자동 감지 또는 명시
- [ ] **갱신 지연 처리** — DR 거래실적은 D-2월 정산. 최신 월 데이터 공백을 정상으로 처리
- [ ] **원본 보존** — 파싱 실패·스키마 변경 대비해 raw 응답 항상 저장
- [ ] **D7 신청 리드타임** — 큐레이션 신청은 승인까지 시간 소요. Phase 0에서 가장 먼저 착수
- [ ] **D8/D9는 정적 데이터** — 매일 수집 스케줄에 넣지 말 것. 연 1회 갱신

---

## 7. 미해결 / 기획팀 확인 필요 항목

코드 작성 전에 답이 필요한 것들. (기획 문서에도 미확정으로 남아있는 부분)

1. **시간별 DR 데이터(D7)를 받을 것인가?** 월별 합산(D5/D6)만으로 모델을 만들 것인지, 시간 단위까지 신청해서 받을 것인지 결정 필요. 후자면 신청 절차 즉시 착수.
2. **업종별 집중도 지수 산출 방법론** — 원시 통계(D8/D9)에서 어떤 공식으로 "지수 3.8"을 만들 것인가. 모델팀과 합의 필요.
3. **업종 DR 적합도(D10) 근거** — 별점·공정 분류를 무엇에 근거해 정의할 것인가(논문/산업자료/도메인 전문가).
4. **지역별 구분** — 기획 문서에서 "KPX 데이터 중 지역별 구분 가능 항목 확인"이 과제로 남아있음(지역 활력 테마 연결용). D1의 육지/제주 외에 더 세분화된 지역 데이터가 필요한지 확인.
5. **실시간 수급 데이터 소스 단일화** — 기획 문서엔 오늘전력수급현황/5분단위/실시간 등 변수가 거의 겹치는 데이터셋이 여러 개 나열돼 있음. 실시간 위험 신호 1개 만드는 데는 하나면 충분. D2로 단일화 권장.

---

## 8. 데이터-기능 매핑 (참고)

| 데이터 | SMP 예측 | DR 낙찰 예측 | 업종 분석 | 실시간 신호 |
|---|:---:|:---:|:---:|:---:|
| D1 SMP+수요예측 | ◎ | ○ | | |
| D2 실시간 수급 | | ○ | | ◎ |
| D3 기상 | ◎ | | | |
| D4 LNG | ◎ | | | |
| D5/D6 DR 거래실적 | | ◎ | | |
| D7 시간별 DR | | ◎ | | |
| D8/D9 업종통계 | | | ◎ | |
| D10 DR 적합도 | | | ◎ | |

◎ 핵심 / ○ 보조

---

*핸드오프 끝. 코드 작성 시작 전 7번(미해결 항목) 먼저 확인 권장.*
